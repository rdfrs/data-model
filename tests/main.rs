use data_model::terms::*;

// TODO: rename main.rs file to describe the integration test, and one per integration test
//  and ensure each one of them has a main function
fn main() {
    let nn_1 = NamedNode::new("https://foo.bar/baz");
}
